export const permission = async () => {
  // const { data } = await useAuth();
  // if (data.value) {
  //   function extractPermissions(permission: any) {
  //     return Reflect.ownKeys(permission).map((key) => permission[key]);
  //   }

  //   // Extract permissions into an array
  //   const permissions = extractPermissions(
  //     data?.value?.permissions
  //   );
  //   const doesUserHaveAnyPermissions = (
  //     all_permissions: string | string[]
  //   ): boolean => {
  //     if (Array.isArray(all_permissions)) {
  //       return permissions.some((permission) =>
  //         all_permissions.includes(permission)
  //       );
  //     }
  //     if (typeof all_permissions === "string") {
  //       return permissions.includes(all_permissions);
  //     }
  //     return false;
  //   };  
  //   return {doesUserHaveAnyPermissions}
  return ''
};
// return { doesUserHaveAnyPermissions: () => false };
 // }
